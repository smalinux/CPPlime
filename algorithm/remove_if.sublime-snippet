<snippet>
	<description>std::remove_if</description>
	<content><![CDATA[
auto ${7:rmd} = remove_if( ${1:v.begin()}, ${2:v.end()}, 
[](int ${3:x}) {
    ${4:return x >= 3;      // if true == remove}
} );
${5:v}.erase(${7:rmd}, ${6:v.end()});
]]></content>
	<!-- Optional: Set a tabTrigger to define how to trigger the snippet -->
	<tabTrigger>remove_if</tabTrigger>
	<!-- Optional: Set a scope to limit where the snippet will trigger -->
	<scope>source.c++</scope>
</snippet>